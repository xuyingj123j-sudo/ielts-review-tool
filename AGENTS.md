# 雅思复习工具 Agent 约束

本文件由立项期临时宪法定稿而来：架构优先、设计优先、真源优先。禁止选项剧场，遵守已确认框架的惯例。每阶段以明确的停止条件收口，用户明确否定的概念必须从代码与文档中删除。

- 默认使用中文；实现与验收以根目录 `SPEC.md` 为唯一产品真源。
- 修改前依次阅读 `SPEC.md`、`dev-docs/README.md` 及其索引的当前文档。
- 业务语义放在核心/服务层；Express 路由和浏览器 UI 不得重复实现 SRS、日期或统计规则。
- 技术栈固定为 Node.js、Express、SQLite、原生 JavaScript SPA；不得加入登录、重型前端框架或构建链。
- UI 必须遵守 SPEC 的暖色渐变、四技能主题色、白色圆角卡片和胶囊导航设计。
- 验收结论必须有当前命令、断言、HTTP 响应、浏览器或服务器状态证据；无法验证必须明确标记。
- 临时截图、日志、打包文件和测试数据库必须放到系统临时目录的任务子目录，并在任务结束前清理。
- 不修改或重启服务器上的其他 PM2 进程；部署进程名固定为 `ielts-review-tool`，端口固定为 3001。
