# 项目边界

## 产品定义

雅思复习工具是面向单个学习者的复习触发器。它统一管理听力、阅读、口语、写作复习卡片，以 Leitner 五箱机制让到期内容自动回到用户面前，并从复习日志聚合进度。

它不是通用笔记软件，不替代 Obsidian，不包含登录鉴权、社交、多用户、考试倒计时或额外提醒系统。

## Idea Shaping

The single recommended mainline is a review trigger rather than another note-taking system. The Potential leverage is the automatic return of due material plus one truthful progress view.

## 4. Non-Goals

不做通用笔记、Obsidian 替代、多用户、登录鉴权、考试倒计时或额外提醒系统。

## 5. First Closed Loop

```text
录入复盘卡片
  -> 当天立即进入到期队列
  -> 翻卡并提交记得/不记得
  -> 服务端更新箱位和下次日期、记录日志
  -> 首页与进度页展示真实聚合数据
```

技术栈、部署目标与产品范围均已由用户在 SPEC 和当前任务中确认。

## 8. Stop Condition

阶段一在 SPEC 的验收标准 1-4 与 7 有可复现证据、PWA 可验证部分完成、手机实机未验证项被明确标注，且源码与服务器部署均存在时结束。

2026-09-04 二次迭代的停止条件：新卡当天入队和旧库安全扩展 `生词` 类型均有自动回归证据，本地开发服务器可访问；线上部署明确留到用户确认后的下一轮。

2026-09-04 三次迭代的停止条件：复习正反面、卡片库正反面及录入/编辑正反字段均按英文字符显示朗读按钮；英式优先、英文降级、连续点击取消前次与无英文语音安全降级均有自动断言；本地 3001 可访问且既有数据库记录数未减少；不执行线上部署。

2026-09-04 五次迭代的停止条件：句子对照统一显示错因；今日任务只含听力/阅读/口语；写作可按时间戳记次并按最近 7 天汇总，目标常量为 1；复盘只展示数据且上一轮判断性逻辑从源码和当前测试中彻底移除；本地 3001 可访问、21 张既有卡片未减少，不执行线上部署。

2026-09-04 六次迭代的停止条件：写作记录必须经过 `validateText` 校验并持久化非空 `content`；不带内容的 HTTP 请求返回 400；首页由按钮展开输入表单，取消或空内容不提交；每周复盘返回并展示最近 7 天每条写作的时间与全文；本地 3001 可访问、21 张既有卡片未减少，不执行线上部署。

2026-09-06 七次迭代的停止条件：`cards.front_audio` 以普通可空 `TEXT` 列安全扩展；新建/编辑 API 能保存该字段；卡片库与复习正面显示 `front`，喇叭优先朗读 `front_audio`并在空值时回退 `front`；完整原句不进入两个页面的可见文本；本地 3001 可访问、21 张既有卡片完整，不执行线上部署。
