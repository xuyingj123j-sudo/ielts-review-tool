# 雅思复习工具 — 建设规格

## 背景

用户当前雅思裸考水平约5-5.5分，目标7分，无固定考试日期。四项学习方法已成熟（听力五步精听法、阅读三栏错题表、口语高阶语料prompt模板、写作Task1/2交替），产出的错词、同义替换对照、句子对照等复盘材料目前记在Obsidian，几乎不会被主动复习。核心痛点不是"没地方存"，是"存了不会自动回来找你"。

本工具要解决的是**复习触发机制**，不是笔记软件。同时吸收原计划中"每日打卡记录+提醒"的功能，不再拆到飞书多维表格，四项雅思相关的数据（复习卡片、每日记录、进度）统一在这一个工具里。

## 核心机制：Leitner五箱间隔重复

- 新卡片进第1箱，每天在复习队列出现
- 复习答"记得" → 升一箱，间隔拉长：第1箱1天 / 第2箱2天 / 第3箱4天 / 第4箱7天 / 第5箱15天
- 复习答"不记得" → 打回第1箱，间隔重置
- 首页每天展示"今日到期卡片数"，用户不用自己判断该复习什么

## 数据模型

### `cards` 表
| 字段 | 类型 | 说明 |
|---|---|---|
| id | int PK | |
| skill | enum | 听力/阅读/口语/写作 |
| type | enum | 同义替换/句子对照/语法修正/听力误听/生词 |
| front | text | 正面：题目表达/错误表达/低阶词 |
| back | text | 背面：原文表达/正确表达/高阶替换词 |
| note | text, 可空 | 补充说明/错因 |
| box | int, 默认1 | 1-5 |
| next_review_date | date | |
| created_at | datetime | |
| last_reviewed_at | datetime, 可空 | |
| review_count | int, 默认0 | |

### `review_logs` 表
| 字段 | 类型 | 说明 |
|---|---|---|
| id | int PK | |
| card_id | FK → cards.id | |
| reviewed_at | datetime | |
| result | enum | correct / incorrect |
| box_before | int | |
| box_after | int | |

仪表盘统计（连续打卡天数、正确率趋势等）由 `review_logs` 聚合查询得出，不额外建统计表——数据量是个人使用级别，实时聚合没有性能问题，没必要为极小数据量维护一张容易和主表不一致的冗余表。

## 页面结构

1. **首页**：今日待复习数、连续打卡天数、各技能卡片总量概览（卡片式布局，见下方UI方向）
2. **复习模式**：翻卡界面，正面→点击翻转显示背面→标记"记得/不记得"→自动下一张，全部完成后显示总结（本轮复习几张、正确率）
3. **录入**：技能+类型选择、正面、背面、备注（选填），保存后 `next_review_date` 设为当天——新卡片创建后必须立即出现在当日复习队列里，可以马上自测。（原规格里"设为次日"的设计已废弃：实测发现用户添加完卡片后本能就想马上测一次，拖到次日反而打断学习动作，不做区分）
4. **卡片库**：列表查看全部卡片，按技能/类型/箱位筛选+搜索，支持编辑/删除
5. **进度仪表盘**：各技能卡片数、已掌握（第5箱）占比、近期复习正确率趋势、连续打卡天数

## UI设计方向

参照用户给出的参考图（Apple健康"摘要"页）：暖色渐变头图 + 白色圆角卡片 + 大号加粗数字 + 彩色图标点缀 + 胶囊形底部导航。转译为女性化、色彩鲜明的风格：

**配色**
- 页面背景：`#FDF6F2`（奶油白，不用纯白）
- 首页头部渐变：`#FFB199 → #FF8FB1`（珊瑚橙→樱花粉）
- 四个技能各固定一个强调色，贯穿全部UI（图标、进度条、标签）：
  - 听力 `#FF8A65`（珊瑚橙）
  - 阅读 `#4ECDC4`（薄荷绿）
  - 口语 `#FF6FA5`（樱花粉）
  - 写作 `#9C8CFB`（薰衣草紫）
- 卡片背景：纯白，圆角16-20px，柔和阴影（`box-shadow: 0 4px 16px rgba(0,0,0,0.06)` 量级）
- 主文字/数字：`#2D2A32`（深灰近黑），数字加粗
- 次要文字：`#8E8A94`（中灰）

**布局细节**
- 统计卡片模式：色块图标（技能色）+ 标签 + 大号加粗数字 + 右侧简易趋势图/箭头，仿参考图"步数3,919"的视觉权重
- 分区标题加粗黑色，右侧可放主题色小字链接（如"查看全部"）
- 底部导航做成胶囊形容器，图标+文字，四个入口（首页/复习/录入/卡片库），当前页高亮为浅色背景+对应主题色图标
- 移动端优先设计，桌面端保持同一视觉语言，卡片改为网格排列而非简单拉伸
- 字体用系统默认无衬线（PingFang SC / 微软雅黑 fallback），标题加粗、正文常规，不用装饰性字体

## 技术方案

- 后端：Node.js + Express + SQLite（单文件数据库），部署到阿里云服务器 `<你的服务器IP>`，PM2常驻——沿用packaging-quote-tool的部署模式，同一台机器新开进程。**端口用3001**（3000已被packaging-quote-tool占用，避免冲突）。
- 前端：原生JS单页应用，不引入重型构建链，响应式布局，PWA manifest支持"添加到主屏幕"
- 不做登录鉴权，不加访问口令（用户已确认）——个人数据，量小，直接开放访问

## API

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /api/cards?skill=&type=&box= | 列表，支持筛选 |
| POST | /api/cards | 新建 |
| PUT | /api/cards/:id | 编辑 |
| DELETE | /api/cards/:id | 删除 |
| GET | /api/review/queue | 今日到期卡片 |
| POST | /api/review/:id | 提交复习结果 `{result: correct|incorrect}`，服务端更新box和next_review_date |
| GET | /api/stats | 仪表盘统计数据 |

## 验收标准（必须给出可grep/可复现的证据，不接受"功能已实现/跑通不报错"）

1. 新建一张听力卡片后，`curl GET /api/cards?skill=听力` 响应JSON必须包含该卡片id和front/back原文——把curl响应贴出来作为证据。
2. 写一个 `test_srs.js`（或同等）自动化脚本：对同一张卡片连续提交3次"correct"，断言box依次变为2→3→4；再提交1次"incorrect"，断言box回到1。脚本跑通并保留输出日志。
3. 构造3张卡片，`next_review_date`分别为今天、昨天、明天，调用 `GET /api/review/queue`，断言返回列表只包含"今天"和"昨天"这两张的id，不包含"明天"那张——写进同一个测试脚本，输出实际返回的id列表。
4. 构造3天连续有 `review_logs` 记录、第4天无记录的场景，调用统计接口，断言"连续打卡天数"计算结果为3；再构造中断后新增1天记录，断言重新计数为1（不是4）——测试脚本+断言输出。
5. 手机浏览器（Chrome移动模式或真机）打开首页，无横向滚动条、底部导航固定可见——截图证据。
6. 浏览器开发者工具Application面板能看到manifest.json且无报错，"添加到主屏幕"可用——截图证据。
7. 部署后 `pm2 list` 显示新进程状态为online，且公网URL可访问——命令输出或截图证据。

## 变更记录（2026-09-04 二次迭代，本轮仅本地修改，不部署）

线上版本（`<你的服务器IP>:3001`）已跑通验收标准1-7并被用户实测，发现两个问题，本轮修：

1. **新卡片不能立即测试**：`createCard` 把 `next_review_date` 设成了次日（沿用了 `nextReview(1, now)` 这个本该用于"复习后"的函数），导致刚录入的卡片当天复习队列里看不到。改成新卡片 `next_review_date` 直接设为当天（`toLocalDate(now)`），`transition`（复习后计算下次日期）逻辑不变。要补一条回归测试：创建卡片后不做任何日期改动，直接调用 `/api/review/queue`，断言新卡片的id在返回列表里。
2. **没有记生词的入口**：`TYPES` 枚举加一个新类型 `生词`（`db.js`、`app.js` 前端下拉都要加），复用现有 front/back/note 字段（front=单词，back=释义，note=例句或搭配），不需要新增字段。**注意**：`type` 列在SQLite里是 `CHECK (type IN (...))` 约束，这台机器上还没有本地数据库文件，但线上服务器的库里已经有用户真实录入的听力卡片——写迁移逻辑时要按"可安全应用在已有数据的库上"来写（建新表复制数据再替换，不能直接假设是空库），即使这轮不部署，也要保证以后部署到线上时不会丢用户已经录入的那张卡片。

**本轮范围限制**：只在本地 `D:\Codex工作区\ielts-review-tool` 修改和测试，**不要**连线上服务器、不要碰PM2、不要部署。改完在本地起一个开发服务器（本地随便一个空闲端口即可，例如3001），供用户在浏览器打开 `http://localhost:<端口>` 直接测试。等用户确认没问题，才进入下一轮"部署到线上"的任务。

## 变更记录（2026-09-04 三次迭代：加朗读功能，仍不部署）

用户明确要求：所有卡片的英文内容（单词/短语/句子）都要能听发音，不只是看文字。

**方案：浏览器原生 Web Speech API（`window.speechSynthesis`）**，不引入云端TTS服务，理由：零成本、零配置、不需要API key和网络请求、离线也能用（用的是浏览器/系统自带语音）。个人学习工具优先选这个，除非用户实际试听后觉得音质不能接受，再考虑升级成云端TTS。

**实现要求：**
1. 在每处展示卡片英文内容的位置加一个小喇叭图标按钮（🔊或等效图标）：复习翻卡界面（正面、背面都要有，因为句子对照类型正反面都是英文）、卡片库列表的每一行、录入/编辑表单里前后字段旁边也可以加（方便她录入时先听一下自己写的对不对）。
2. 喇叭图标只在该字段文本包含英文字母时显示，纯中文内容（比如生词类型的back中文释义）不显示喇叭——用简单正则判断文本是否含 `[a-zA-Z]` 即可，不用做语言检测库那么复杂。
3. 点击喇叭调用 `speechSynthesis.speak()` 朗读该字段全文，默认语音优先选英式英语（`en-GB`），如果设备没有英式语音就自动降级用任意英文语音（`lang` 以 `en` 开头即可），都没有的话按钮可以置灰或不显示，不要报错崩溝。
4. 要处理重复点击/连续点击的情况：新的朗读请求应该打断上一个还没播完的（调用前先 `speechSynthesis.cancel()`），不要让多个朗读请求排队叠加着放。
5. 这个功能纯前端，不需要后端改动、不需要新的API接口。

**验收要求（同样要给可复现证据）：**
- 说明具体改了哪些前端文件、哪几行
- 用 Node 或浏览器自动化的方式验证：`speechSynthesis.speak` 被正确调用了（比如可以在测试里mock `window.speechSynthesis`，断言点击喇叭后 `speak` 被调用且传入的文本内容和该卡片字段一致）
- 纯中文字段（生词类型的back）confirm 不出现喇叭按钮——写个断言或至少截图验证
- 仍然本地不部署，本地开发服务器保持可访问，供用户直接在浏览器里试听

## 变更记录（2026-09-04 四次迭代：字段标签+每日任务+每周复盘/阶段建议，仍不部署）

### 1. "句子对照"类型的note字段，按技能显示不同标签

目前note字段UI上统一叫"备注"。当 `skill=阅读` 且 `type=句子对照` 时，字段标签改显示"错因"（对应用户阅读方法论里的"题目表达｜原文表达｜错因"三栏表，front=题目表达，back=原文表达，note=错因）；其他技能/类型组合保持"备注"不变。这只是前端展示标签的条件文案，不改数据结构，note字段本身存什么内容不做校验限制。

### 2. 每日任务清单

新增数据表 `daily_tasks`：
| 字段 | 说明 |
|---|---|
| id | PK |
| task_date | 日期（YYYY-MM-DD） |
| skill | 听力/阅读/口语/写作 |
| done | boolean |
| completed_at | 完成时间，可空 |

行为：每天首次访问时，如果当天四个技能的任务行不存在，自动创建（done默认false）。首页展示"今日任务"四个可勾选项，勾选后调用接口更新done和completed_at。

API：
- `GET /api/tasks/today` — 返回当天四项任务状态，不存在则自动创建后返回
- `POST /api/tasks/:id/toggle` — 切换done状态

### 3. 每周复盘页面

新增页面/入口"本周复盘"，需要的数据：
- 本周（周一到今天，或最近7天，用最近7天更简单，不用处理"周一为始"的边界问题）每个技能 `daily_tasks.done=true` 的天数 / 7
- 本周每个技能的复习正确率——这是新的统计口径，现有 `reviewStats`/`stats()` 里的 `recentAccuracy` 是不分技能的整体正确率，需要新增按技能拆分的版本（`review_logs` 表本身没有skill字段，需要 JOIN `cards` 表按 `cards.skill` 分组统计最近7天的正确率）
- 各技能掌握率（已有 `skills[skill].masteredRate`，直接复用）

API：`GET /api/stats/weekly` — 返回上述三组数据，结构自定，前端能渲染出一个"本周复盘"表格/卡片组即可。

### 4. 阶段建议（基于真实复习数据的参考提示，不是权威判定）

规则（简单阈值，纯前端或后端算都行，写在一处方便以后调参）：
- 该技能本周正确率 ≥ 80% 且完成天数 ≥ 5/7 → 提示"进展稳定，可以考虑加量或推进下一阶段"
- 该技能本周正确率 < 50% 或完成天数 < 3/7 → 提示"这周有点吃力，建议先别加新内容，巩固现有的"
- 其余情况 → 提示"继续保持当前节奏"
- 数据不足时（本周该技能完全没有复习记录）→ 提示"本周还没有复习记录，暂时无法给出建议"

**UI文案必须包含一句免责说明**，大意是："这是基于你在本工具里的复习数据给出的参考建议，不能替代真实计时模考的判断，建议定期用真实模考交叉验证。"——这句话不能省略，避免用户误以为这是权威的雅思阶段判定。

### 本轮范围限制（同前）
只在本地 `D:\Codex工作区\ielts-review-tool` 修改和测试，不连线上服务器、不部署、不碰PM2。本地开发服务器数据库现有21张真实卡片数据，不能清空或重置。改完确保 `http://localhost:3001` 仍可访问，数据完整。

## 变更记录（2026-09-04 五次迭代：修正任务模型+去掉阶段建议，仍不部署）

用户对上一轮（四次迭代）的设计提出了修正，这轮按新指示改，**覆盖**上一轮部分设计：

### 1. note字段标签，简化成按类型判断（不再叠加技能条件）
`type=句子对照` 时一律显示"错因"，不管skill是听力还是阅读还是别的；其余类型（同义替换/生词/语法修正）仍显示"备注"。去掉上一轮"仅阅读+句子对照才显示错因"这个多余的技能条件。

### 2. 每日任务只有三项，不含写作
`daily_tasks` 每天只生成**听力、阅读、口语**三条（如果上一轮已经把写作也做成每日任务，这轮要去掉写作那一条，历史上如果已经生成过带写作的每日任务记录，允许保留历史数据不强制清理，但从这轮起不再新生成写作的每日任务行）。

### 3. 写作改成"每周任务"，不是每日打卡
新增一个轻量的写作完成记录方式（比如一个"记一次写作"按钮，点击即记录当前时间戳），不需要每天出现在待办里。目标次数设一个常量 `WEEKLY_WRITING_TARGET = 1`（写在代码里显眼位置、方便以后改成2或3，不用做成用户可调的设置界面，这是给以后改代码用的，不是给用户的设置项）。每周复盘页面显示"本周写作：完成N次 / 目标1次"，N是最近7天内写作记录的次数。

### 4. 每周复盘页面内容（最终版，覆盖上一轮设计）
只展示数据，不生成任何"建议"文字：
- 听力/阅读/口语 三项，本周（最近7天）每日任务完成天数：X/7
- 听力/阅读/口语/写作 各技能本周复习正确率（写作没有"复习卡片"这个概念，如果没有对应复习数据就显示"暂无数据"，不用勉强凑一个数字）
- 各技能卡片掌握率（第5箱占比，复用现有逻辑）
- 本周写作完成次数 / 目标次数

### 5. 完全去掉"阶段建议"功能
不要任何阈值判断、不要生成"进展稳定"/"建议巩固"这类文字、不需要免责声明文案。用户自己看数据判断要不要进入下一阶段，工具只负责把数据摆出来，不替她下结论。如果上一轮已经写了这部分代码（规则引擎、建议文案、免责声明），这轮要删掉，不要留着没用到的死代码。

### 本轮范围限制（同前）
只在本地 `D:\Codex工作区\ielts-review-tool` 修改和测试，不连线上服务器、不部署、不碰PM2。本地开发服务器数据库现有21张真实卡片数据，不能清空或重置。改完确保 `http://localhost:3001` 仍可访问，数据完整。


## 变更记录（2026-09-04 六次迭代：写作记录要存内容，不是纯点击计数，仍不部署）

用户反馈：上一轮"记一次写作"做成了点一下就+1的计数器，不对——她想要的是点进去能把这周写的作文/书信内容实际输入进去，记录的是有内容的一条写作记录，不是空的次数。

### 改动

1. `writing_completions` 表加一个 `content` 字段（TEXT NOT NULL），跟卡片的 front/back 一样限长（不超过5000字，复用现有 `validateText` 校验逻辑）。
2. `POST /api/writing/complete` 请求体必须带 `content`，内容为空要拒绝（400），不能再是"不带参数点一下就成功"的裸计数接口。
3. 前端"记一次写作"从"点击按钮直接触发请求"，改成"点击后展开一个文本输入框（textarea），用户输入/粘贴这周写的内容，再点确认提交才真正记一条"。取消/不填内容就不产生记录。
4. `GET /api/stats/weekly` 的写作部分，除了保留"完成N次/目标1次"，还要把本周（最近7天）每条写作记录的 `completed_at` 和 `content` 一起返回，前端在"本周写作"卡片里列出来（时间+内容，内容不用截断预览，反正一周顶多1-3条，直接展示全文即可，不用做成折叠/详情弹窗那么复杂）。
5. 已经在上一轮产生的测试数据（如果有不带content的旧记录）不用做数据迁移清理，允许历史脏数据保留，只保证新逻辑往后写入的都必须带content。

### 本轮范围限制（同前）
只在本地 `D:\Codex工作区\ielts-review-tool` 修改和测试，不连线上服务器、不部署、不碰PM2。本地开发服务器数据库现有21张真实卡片数据，不能清空或重置。改完确保 `http://localhost:3001` 仍可访问，数据完整。

## 变更记录（2026-09-06 七次迭代：正面"挖空显示"与"完整语音"分离，仍不部署）

用户使用场景：听力里有一个词没听清，想做成"听力误听"类型的卡片——正面是挖空后的原句（比如 `She often goes to a ______ near her house.`），不能让正面显示答案；但正面需要能点喇叭听到**含答案的完整原句语音**（比如 `She often goes to a keep-fit studio near her house.`），这样才能真正做"听写后自查"的练习。背面是正确答案本身。

**问题**：现有朗读功能是"读什么就是显示的那段文字"，如果正面文字本身是挖空句，点喇叭只会读出挖空句（下划线部分念不出东西），没法听到完整句子。需要把"显示文字"和"朗读文字"拆成两个独立字段。

### 改动

1. `cards` 表新增一个可空字段 `front_audio`（TEXT，可为空）。这是普通 `ALTER TABLE cards ADD COLUMN front_audio TEXT` 级别的迁移，不涉及CHECK约束，不需要像之前type字段那样做"建新表复制数据"的复杂迁移，正常加列，允许为空，不影响现有21张卡片（历史卡片这个字段是null）。
2. 录入/编辑表单，正面字段下方加一个可选的补充输入框，标签类似"正面朗读用完整原句（可选，含答案，只用于生成语音，不会显示成文字）"，用户填了就存进 `front_audio`，不填就是null。这个输入框任何技能/类型都可以用，不用限制成"仅听力误听"专属（因为句子对照类型未来说不定也想这样用，不用现在就写死限制）。
3. 朗读逻辑调整：不管是复习翻卡界面还是卡片库列表，**正面**的喇叭图标：如果 `front_audio` 有值，点击朗读 `front_audio` 的内容；如果 `front_audio` 为空，退回朗读 `front` 本身（跟现在的行为一致，向后兼容）。**背面**朗读逻辑不变，一直读 `back`。
4. 喇叭图标"是否显示"的判断也要跟着调整：只要 `front_audio`（如果有）或 `front`（如果没有 `front_audio`）任一含英文字母，就显示正面喇叭——不能因为显示的挖空句里只有下划线和一两个英文单词就误判"没有英文内容"从而不显示喇叭。
5. 页面上任何地方都不能把 `front_audio` 的完整文字显示出来（包括卡片库列表、复习翻卡界面），它只作为朗读的音源，不作为可见文本渲染——**除了**编辑表单里她自己回来改这张卡片时，输入框要能看到并编辑自己之前存的内容（不然没法修改）。

### 验收要求（可复现证据）
- 新建一张 `type=听力误听` 的卡片，`front` 填挖空句，`front_audio` 填完整原句，`back` 填答案——curl创建后，响应JSON里 `front_audio` 字段要能读到写入的完整原句。
- 写测试断言：mock `speechSynthesis`，验证点正面喇叭时传给 `speak()` 的文本是 `front_audio` 的内容而不是 `front` 的内容。
- 再测一张没填 `front_audio` 的卡片（比如现有21张老卡片里随便一张），验证点正面喇叭时传给 `speak()` 的是 `front` 本身——确认向后兼容没坏。
- 截图或DOM检查确认卡片库列表和复习界面里，`front_audio` 的原文没有以任何形式显示成可见文字。
- 确认21张老卡片数据完整（curl数量证据）。

### 本轮范围限制（同前）
只在本地 `D:\Codex工作区\ielts-review-tool` 修改和测试，不连线上服务器、不部署、不碰PM2。本地开发服务器数据库现有21张真实卡片数据，不能清空或重置。改完确保 `http://localhost:3001` 仍可访问，数据完整。

## 上线 + 建私有GitHub仓库（2026-09-06）

用户本地验收满意，现在要做两件事：
1. 把本地当前版本（含七轮迭代全部功能：Leitner复习、朗读、生词/听力误听类型、front_audio、每日任务、写作内容记录、每周复盘）部署到线上服务器，替换掉之前只做过一次的旧版本。
2. 把代码推到GitHub私有仓库，不能公开。

### GitHub部分
- 这个目录还不是git仓库，需要 `git init`。
- `.gitignore` 已经排除了 `data/*.db` 和 `data/*.db-*`——本地真实卡片数据不会被提交，这是对的，保持不变。
- `gh` 已经登录（账号 xuyingj123j-sudo，有repo权限），用 `gh repo create` 创建**私有**仓库并推送，仓库名用 `ielts-review-tool`。创建后必须用 `gh repo view --json visibility` 之类的方式验证一下确实是private，不是public，把这个验证结果作为证据。
- 提交信息正常写就行，不用刻意隐藏这是AI辅助写的。

### 部署部分
- 线上服务器 `<你的服务器IP>`，SSH免密 `root@<你的服务器IP>`，现有PM2进程叫 `ielts-review-tool`（端口3001），这是最早第一轮部署时的旧版本代码+旧数据库（里面只有用户当时手动测试加的1张卡片）。
- **数据处理**：本地 `data/ielts.db`（16张真实卡片，是用户这几天在本地反复验证保留下来的最终版本）应该成为线上的正式数据，覆盖掉线上那个只有1张测试卡片的旧库。覆盖前**必须先把线上现有的db文件备份一份**（改个带时间戳的文件名留着，不要直接删），防止万一。
- 部署方式沿用之前的模式（tar打包代码上传，服务器端 `npm install` 不要用 `--omit=dev`），用新代码替换旧代码，本地的 `data/ielts.db` 一并上传替换线上的（备份旧的之后）。
- 重启PM2的 `ielts-review-tool` 进程（用新代码），不要碰同服务器上的 `packaging-quote-tool` 和 `iris-wechat-relay`。
- 部署后验证：`pm2 list` 显示online；公网 `http://<你的服务器IP>:3001` 能访问；卡片数量是16张（跟本地一致）；抽查一张有 `front_audio` 的卡片，确认线上朗读逻辑生效（不是跑的还是旧代码）；每日任务、每周复盘接口能正常访问。

### 报告要求
- GitHub仓库地址，以及确认private的证据
- 部署后的验证结果（每一项都要具体证据，不要笼统说"部署成功"）
- 服务器上旧数据库备份文件的具体路径

## 加HTTPS，解决iOS"添加到主屏幕"打不开的问题（2026-09-06）

现象：`http://<你的服务器IP>:3001` 在Safari标签页里能正常打开，但用户把它"添加到主屏幕"后，从主屏幕图标启动就会报错"导览失败，因为要求是针对已启用仅限HTTPS的HTTP URL"。根因：iOS对`display:standalone`的主屏幕网页应用（PWA）在独立启动时，比普通标签页浏览更严格地要求HTTPS，纯HTTP的站点在主屏幕图标这种启动方式下大概率会被拦，这是iOS系统层面的限制，不是这个应用代码的问题。

现在服务器只有裸IP没有域名，正常申请HTTPS证书需要域名。解决方案：用 `nip.io` 这个通配符DNS服务（`<你的服务器IP>.nip.io` 会自动解析回 `<你的服务器IP>` 这个IP，不用买域名、不用备案、不用改DNS），配合在服务器上装 **Caddy** 作反向代理——Caddy能自动向Let's Encrypt申请免费证书并自动续期，不用手动管理证书。

### 具体要求
1. 确认服务器 `<你的服务器IP>` 上80、443端口目前没有被占用（已经检查过，nginx未安装、这两个端口目前是空的），装Caddy不会跟现有服务冲突。
2. 安装Caddy（Debian可以用官方仓库或者直接下载二进制，选一种稳妥的方式，装完确认 `caddy version` 能正常输出）。
3. 配置Caddyfile：站点是 `<你的服务器IP>.nip.io`，反向代理到 `localhost:3001`（也就是现在这个雅思复习工具的PM2进程端口）。不要动 `packaging-quote-tool`（3000端口）和 `iris-wechat-relay`，Caddy只处理这一个新增的域名转发。
4. 启动Caddy并设置成开机自启（用systemd管理，跟其它服务一样能长期稳定跑，不是临时进程）。
5. 阿里云轻量应用服务器的防火墙80、443端口之前已经放行过（用户截图确认过），不用再改；如果Caddy启动后发现连不通，检查一下是不是这两个端口被别的规则挡住了。

### 验收标准（可复现证据）
1. `curl -I https://<你的服务器IP>.nip.io` 返回200，且响应头能看出是HTTPS（不是自签名证书报错，是Let's Encrypt签发的有效证书——用 `curl -v` 看证书颁发者验证）。
2. `curl -I http://<你的服务器IP>.nip.io` 应该被自动跳转到https（Caddy默认行为），或者至少不报错。
3. 页面内容和 `http://<你的服务器IP>:3001` 完全一致（同一个应用，只是换了访问方式），卡片数量、朗读功能等不受影响——curl `/api/cards` 数量对一下。
4. `systemctl status caddy` 显示active/running，且是enabled（开机自启）。
5. 确认 `packaging-quote-tool` 和 `iris-wechat-relay` 两个PM2进程没有被影响，`pm2 list` 三个进程都在线。

### 报告要求
- 最终给用户的新访问地址（应该是 `https://<你的服务器IP>.nip.io`，不用带端口号）
- 每条验收标准的具体curl输出/命令输出
- 明确告诉用户：换成这个新地址后，之前那个 `http://<你的服务器IP>:3001` 还能不能继续用（大概率还能用，只是没有HTTPS，主屏幕图标建议用新地址重新添加一次）

## 听力真题练习模块（2026-09-06）

新增一个跟错词卡片体系并列的子模块，专门用于"整份听力测试反复精听练到达标"，对应用户原有方法论：每天练一个Section（来自不同test），直到该Section阶段整体稳定7-8/10才解锁下一个Section阶段。不做自动判分，用户自己听完按答案自评后手动记分。

### 解锁维度：按Section阶段，不是按Test
- Section1阶段：默认解锁，所有test的Section1都能自由练，不分先后顺序
- Section2阶段：Section1阶段"整体达标"后解锁
- Section3阶段同理由Section2解锁，Section4由Section3解锁
- Test编号（剑16/17/18、Test1-4）只是给内容分类的标签，用于筛选浏览，不是解锁门槛本身

"整体达标"的判定规则（写成常量，方便以后调，不用做成用户可调的设置界面）：
```
SECTION_UNLOCK_STREAK = 3
```
该Section阶段最近3条练习记录，必须**同时满足**：自评分数≥70%（`SECTION_UNLOCK_THRESHOLD = 0.7`，也写成常量）、且5项达标标准全部勾选——3条都满足才解锁下一阶段。这是纯数字/布尔值比较，不是AI生成的判断文字。

### 数据模型
`listening_tests` 表：
| 字段 | 说明 |
|---|---|
| id | PK |
| source_book | 例如"剑16" |
| test_number | 1-4 |

`listening_sections` 表：
| 字段 | 说明 |
|---|---|
| id | PK |
| test_id | FK → listening_tests |
| section_number | 1-4，对应Part1-4 |
| title | 该part的主题，例如"Children's Engineering Workshops" |
| audio_path | 音频文件在服务器上的相对路径 |
| transcript_text | 听力原文全文 |
| answer_key_text | 该part的题目与答案参考文本 |

`listening_attempts` 表：
| 字段 | 说明 |
|---|---|
| id | PK |
| section_id | FK → listening_sections |
| attempt_date | 日期 |
| score_correct | 本次自评对了几题 |
| score_total | 本次总题数（一般是10，留灵活） |
| check_gist | 原文大意能看懂，布尔 |
| check_key_sentences | 关键句能听懂，布尔 |
| check_paraphrase | 题目改写能认出来，布尔 |
| check_redo_improved | 错题重新做能明显提高，布尔 |
| check_retention | 过两三天再听仍能听懂大部分，布尔 |
| notes | 选填，自由文本 |

### 音频文件存储
- 服务器上单独开一个目录存音频文件（不要放进代码仓库git版本控制范围，`.gitignore`要排除），比如 `data/audio/`
- 提供一个静态文件服务路由，只用于这个工具自己的页面播放，不做成任何可被搜索引擎索引或外部直接罗列目录的公开入口

### 页面
1. **听力练习首页**：显示当前解锁到哪个Section阶段，每个阶段下列出该阶段所有test的对应Section（哪些已经练过、最近一次分数），锁住的阶段整体置灰、显示"还差几条达标记录解锁"
2. **单个Section练习页**：音频播放器 + 原文/答案参考文本（默认隐藏，按钮切换显示，配合五步法第2/3/4步）+ 底部记录表单（分数、5个勾选框、备注），提交后写入 `listening_attempts`，同时刷新该Section阶段的解锁进度

### 本轮试运行范围（验证设计用，不用一次性导入全部）
先只导入 **剑16 Test1**（Part1-4共4条）作为试运行数据，验证音频播放、原文展示、记分、解锁进度计算这一整条链路能跑通。素材来源：
- 音频：`D:\XZ\雅思学习资料\剑16\音频\Test1_Part1.mp3` 到 `Test1_Part4.mp3`
- 原文与答案：`D:\XZ\雅思学习资料\剑16\剑16_听力原文与答案.html`，需要写脚本解析出Test1这4个Part各自对应的原文和答案文本段落（该HTML按`<div class="part">`分段，每段有`<h2>`标题如"Test 1 · Part 1 | Children's Engineering Workshops"，找Test1相关的4段解析出来即可，其余Test2-4的先不用解析，等这轮验证通过后再批量做）

### 约束
- 全程只用户自己已有的本地文件，不要联网搜索或下载任何额外的听力测试材料
- 音频/原文/答案这类版权材料，任何情况下都不能出现在GitHub仓库里（包括私有仓库），`.gitignore`要明确排除音频目录和跟这批版权材料相关的数据
- 不要影响现有的错词卡片、每日任务、写作记录等功能，这是新增的并列模块

### 验收标准
1. 剑16 Test1的4个Part数据正确导入（curl查询能看到4条listening_sections记录，标题内容对得上html里的Test1 Part1-4标题）
2. 音频文件能通过工具页面正常播放（本地或线上环境，能给出音频文件HTTP可访问的证据）
3. 提交一条练习记录（含5个勾选框全部为true、分数8/10），验证Section1阶段进度正确计算——单独这一条不够解锁（因为需要连续3条），补够3条同样达标的记录后，验证Section2阶段从锁定变为解锁状态，给出具体的解锁判定前后对比证据
4. 提交一条不达标的记录（比如只勾了3个checklist或分数低于70%），验证不会被误判为达标
5. 确认 `.gitignore` 已正确排除音频目录，`git status`不会显示这些音频文件为待提交
6. 现有的21+张错词卡片、每日任务、写作记录功能不受影响（跑一下 test_srs.js 之类既有测试确认还是全绿）

## 听力练习加填空自动判分（2026-09-06 追加）

用户确认用"原文自动挖空"方案（不做PDF OCR，不用手动录入题目）：把transcript_text里`(Qn)`标记紧邻前面的答案词/短语挖空成填空框，其余原文照常显示，用户边听边填，提交后自动跟answer_key_text核对，自动算分。

### 不新增持久化的题目表
不建`listening_questions`表——挖空结构在读取/判分时从`transcript_text`和`answer_key_text`即时解析即可，避免跟原文数据产生不同步风险。

### 解析规则（要处理这批真实数据里已经出现的两种边界情况）
1. `answer_key_text` 格式是逐行 `数字. 答案`，比如：
   ```
   1. egg
   2. tower
   ...
   6. movie/film
   ```
   解析成 `{1: 'egg', 2: 'tower', ..., 6: ['movie','film']}`——注意第6条这种带`/`的是"多个可接受答案"，不是字面答案的一部分，要按`/`拆开存成数组，判分时命中任意一个都算对。

2. `transcript_text` 里答案文字和`(Qn)`标记之间可能隔着换行和空格，例如原文里实际是：
   ```
   goes round an
   egg (Q1)
   ```
   要挖空的是"egg"这个词（不含换行符），不是"an\negg"整段。解析时按"从`(Qn)`往前找，越过空白字符，匹配到`answer_key_text`里对应编号的答案文本（忽略大小写和首尾空白），挖空这段文本"来定位，不要用固定字符数往前截。

3. 返回给前端的结构：把transcript_text切割成一个有序片段数组，比如 `[{type:'text', value:'...design a special cover that goes round an '}, {type:'blank', number:1}, {type:'text', value:'\n, so that when...'}, ...]`，前端按这个数组渲染，`blank`片段渲染成一个带编号标签的输入框，其余`text`片段正常显示（保留说话人/换行等原有格式）。

### 判分接口
新增（或者调整现有提交attempt的接口）：接收用户对每个空的填写 `{1:'egg', 2:'tower', ...}`，服务端逐题核对（忽略大小写、首尾空白，命中`/`拆分出的任意一个可接受答案都算对），返回：
- 每题对错和正确答案（用于展示"❌ 你填的是X，正确答案是Y/Z"）
- 自动算出的 `score_correct` / `score_total`，直接写入`listening_attempts`（不用再手动输入分数）
- 5项checklist依然由用户手动勾选，跟之前设计一致，不受这次改动影响

### 页面调整
练习页原来"原文/答案默认隐藏，按钮切换显示"这部分逻辑要跟填空题共存——建议默认展示的是"填空版原文"（挖空的那些位置是输入框，其余照常显示），提交后原地把输入框替换成"你的答案 vs 正确答案"的对错展示，不用跳转新页面。原来的"答案参考文本"按钮可以保留作为兜底（万一她想不做填空直接看完整答案列表）。

### 验收标准（用剑16 Test1 Part1这条真实数据做断言，已知它的10个答案是：1.egg 2.tower 3.car 4.animals 5.bridge 6.movie/film 7.decorate 8.Wednesdays 9.Fradstone 10.Parking）
1. 解析出的挖空片段数组，10个blank的number依次是1到10，不多不少
2. 提交全部正确答案（包括第6题故意测两种写法：一次提交"movie"、一次提交"film"，两次都应判定为对），验证 `score_correct=10, score_total=10`
3. 提交几个错误答案，验证对应题目标记为错，且返回的"正确答案"字段跟answer_key_text一致
4. 大小写/首尾空格容错：提交" Egg "（带空格且大小写不同）应该仍判定为对
5. 现有5项checklist和其余listening功能（阶段解锁逻辑）不受影响，跑一下已有的听力模块测试确认还是全绿
6. 现有错词卡片、每日任务、写作记录功能回归测试通过

不部署，不推GitHub，本地验证即可。

## 卡片拼写测试模式（2026-09-07）

用户在听力做题中发现自己有些词是拼写错误（不是听不懂/不认识），需要针对这类词做拼写专项练习。用户已确认方案：给单张卡片加一个可选"拼写测试模式"，复用听力填空判分那套逻辑，不新建判分体系。

### 数据模型
`cards` 表加一列 `review_mode`（TEXT，默认 `'flip'`，可选 `'spelling'`）。已有卡片不受影响（默认还是flip，普通ADD COLUMN迁移，不涉及CHECK约束重建）。

### 判分逻辑复用
把听力挖空判分那部分比较函数（大小写不敏感、首尾空格trim、`/`分隔多个可接受答案任一命中即算对）抽成一个双方共用的公共函数（放进一个共享的domain模块，比如 `src/domain/answerMatch.js`），听力模块和卡片拼写模式都调用同一个函数，不要各写一份、逻辑长歪。

### 录入/编辑表单
加一个开关"拼写测试模式"（默认关，即普通flip模式）。开了之后这张卡在复习时走新流程。

### 复习流程（仅对 review_mode='spelling' 的卡片生效，其余卡片完全不变）
1. 显示卡片正面（front）内容作为提示，跟普通卡片一样
2. 一个喇叭按钮，点击朗读**背面（back）**内容的语音——注意是朗读back，但**不显示back的文字**（跟front_audio"只朗读不显示"是同一个原则，这里反过来是"back只朗读不显示"）
3. 一个文本输入框，用户输入她认为的正确拼写
4. 提交后，用公共判分函数核对输入内容跟back是否匹配（back本身如果包含`/`表示多个可接受写法，处理方式跟听力那边一致）
5. 判对：自动按"记得"处理（Leitner箱位+1，正常走`transition`逻辑）；判错：自动按"不记得"处理（退回第1箱），并把正确拼写（back原文）展示出来
6. 全程不需要用户自己点"记得/不记得"按钮，对错由系统判定

### 验收标准
1. 新建一张 `review_mode='spelling'` 的卡片（用现有卡片库里任意一张生词卡改成spelling模式亦可），复习时提交正确拼写，验证box自动+1（走的是correct分支）
2. 同一张卡提交错误拼写，验证box自动回到1（走incorrect分支），且返回的响应里包含正确的back原文，可以在界面上显示出来
3. 提交答案时验证不显示back文字给用户（复习界面在提交前的DOM/接口响应里，不能提前泄露back内容）
4. 验证公共判分函数被听力模块和卡片拼写模式两处调用（不是各自重复实现），可以贴出这个共享文件的引用位置作为证据
5. 大小写/空格容错、`/`多答案容错，用一条实际卡片数据跑通（跟听力那边同样的边界情况）
6. 普通flip模式的卡片复习流程完全不受影响，现有18张卡片默认还是flip，回归测试跑一遍确认

不部署，不推GitHub，本地验证即可。

## 自由练习模式（补做，2026-09-04确认设计、2026-09-07才实际派发）

用户此前确认的设计，之前遗漏未派发，现在补上：

### 1. 今日复习清空后，能继续自由练习
现在"今日复习"队列清空后只显示"录入卡片"一个选项。要加一个"自由练习"按钮，点击后进入翻卡界面，可以选择练习范围（比如"全部卡片"或"今天已复习的卡片"，具体选项UI自行合理设计，不用太复杂），翻卡界面跟正式复习一样（前面看正面，翻卡看背面，或者是spelling模式就走拼写流程——如果这张卡是review_mode='spelling'，自由练习模式下也要能走拼写测试的界面，不是所有卡片都退化成flip）。

**关键：自由练习模式下的所有操作，都不能影响box和next_review_date**，也不能写入review_logs（不算作正式复习记录，不影响连续打卡天数统计）。可以无限次重复练，纯粹自测巩固用。

### 2. 卡片库点击某张卡片，直接进入该卡片的练习
现在点击卡片库列表里的卡片没有任何反应。要加上：点击一张卡片，跳转到跟上面"自由练习"同样性质的单卡练习界面（只针对这一张卡），同样不影响box/next_review_date/review_logs，纯自测。

### 复用
自由练习模式和正式复习共用同一套UI组件（flip翻卡界面、spelling拼写界面），只是数据提交路径不同——正式复习提交会调用真正修改box的接口，自由练习/单卡练习提交只做前端展示对错，不调用会修改数据库box状态的接口（或者调用一个专门的"仅返回结果不落库"的只读判分接口，两种实现方式都可以，自行选择更简洁的）。

### 验收标准
1. 今日复习清空后，页面上出现"自由练习"入口（截图或DOM证据）
2. 点击自由练习，进入翻卡/拼写界面，能正常完成一轮练习
3. 用curl验证：自由练习流程中提交答案后，对应卡片的box、next_review_date在数据库里没有变化（前后对比）
4. 卡片库任意一张卡片点击后能跳转到该卡片的练习界面
5. 单卡练习同样验证不影响box/next_review_date（curl前后对比）
6. 正式的"今日复习"队列流程完全不受影响，该修改box的还是正常修改，回归测试确认
7. 现有全部功能（错词卡片、每日任务、写作记录、听力模块、拼写模式）回归测试通过，18张卡片数据不变

不部署，不推GitHub，本地验证即可。

## 数字听力专项模块（2026-09-09，仿照Numblr App设计）

用户参考了iOS App "Numblr"（专门练英语数字听力：数字/日期/时间/金额/电话号码，还有把数字嵌进真实对话场景里练的"对话测验"），要求在本工具里照着做一套等价功能，且用户明确要求**对话测验里数字相关的测验类型全部都要**，不是挑几个做。这是跟现有卡片/听力真题模块**并列的新模块**，不改动、不影响任何现有功能。

不做任何PRO/付费墙——Numblr里标"PRO"的功能，在这里全部默认解锁。

### 术语约定
- **category（基础类别）**：`number`(一般数字) / `date`(日期) / `time`(时间) / `money`(金额) / `phone`(手机号码)，共5个，是所有出题的底层数据类型。
- **mode（出题模式）**：`standalone`(独立数字测验，纯听数字/日期/时间/金额/电话号码本身，不套句子) / `dialogue`(对话测验，把数字嵌进一句完整的英文语境句子里听) / `exam`(考试模式，限时+限重播，从所有category混合出题)。
- **subtype（对话测验的场景子类型）**：仅 `mode='dialogue'` 时有值，对应Numblr对话测验里的10个入口：`mixed`(混合) / `general`(一般数字) / `money`(金额) / `phone`(手机号码) / `birthday`(生日) / `date_of_birth`(出生日期) / `deadline`(截止日期) / `anniversary`(纪念日) / `movie_release`(电影上映) / `date`(日期)。每个subtype对应一个底层category（`general`→number，`money`→money，`phone`→phone，其余6个日期相关subtype→date），`mixed`则每题随机挑一个subtype。

### 数据模型
新增 `number_drill_attempts` 表（只在提交答案时写入，出题阶段不落库）：
| 字段 | 类型 | 说明 |
|---|---|---|
| id | int PK | |
| mode | text | standalone/dialogue/exam |
| category | text | number/date/time/money/phone |
| subtype | text, 可空 | 仅dialogue模式有值 |
| prompt_text | text, 可空 | 对话模式的完整语境句子（数字部分是自然语言表达，不是"___"占位），standalone模式为NULL |
| spoken_text | text | 实际喂给speechSynthesis朗读的文本（可能等于prompt_text，也可能是纯数字/日期短语） |
| correct_answer | text | 标准化后的正确答案 |
| user_answer | text | 用户提交的原始输入 |
| is_correct | integer | 0/1 |
| exam_session_id | text, 可空 | 仅exam模式内的题目共享同一个session id，用于算总分 |
| attempted_at | text | |

不新建"待作答题目"表——出题和判分之间的临时状态用**进程内内存Map**（key是`crypto.randomUUID()`生成的questionId，value是`{correctAnswer, category, promptText, spokenText, createdAt}`），10分钟未提交自动过期清理（每次请求顺手清一遍过期条目即可，不用定时器）。重启服务会丢失未提交的题目属于预期行为（练习中的单题不算正式数据，不需要持久化）。

### 出题与判分核心逻辑：`src/domain/numberDrill.js`

导出两个函数：
- `generateQuestion({ category, subtype, mode, now })` → 返回 `{ category, subtype, mode, spokenText, promptText, correctAnswer }`。`now`用于让测试能注入固定时间保证可复现（比如date类别如果需要"最近几年"这种相对范围，测试时不应该依赖真实系统时间）。
- `gradeAnswer({ category, correctAnswer, userAnswer })` → 返回 `boolean`。

**各category规则（务必按下面的例子实现，验收标准会精确断言这些例子）：**

1. **number（一般数字）**
   - 随机生成一个2到7位的整数（位数随机），转成字符串。
   - `spokenText` = 数字本身的字符串（如`"4829"`），直接交给浏览器TTS朗读（TTS会自动读成"four thousand eight hundred twenty-nine"这种自然表达，这正是要练的听力点）。
   - `correctAnswer` = 该数字字符串（无千分位逗号），如`"4829"`。
   - 判分：把用户输入去掉所有非数字字符（空格、逗号）后与correctAnswer比较。

2. **date（日期）**
   - 随机生成 day(1-31，要用真实的月份天数校验，比如2月不能生成30号)、month(1-12)、year(1960-2035)。
   - 需要实现 `ordinal(day)`：1→"first" 2→"second" 3→"third" 4→"fourth"...11→"eleventh" 12→"twelfth" 13→"thirteenth"...21→"twenty-first" 22→"twenty-second"...31→"thirty-first"（注意11/12/13这种不能套用"个位数决定后缀"的通用规则）。
   - 需要实现 `yearToWords(year)`：如`2019`→`"twenty nineteen"`，`1990`→`"nineteen ninety"`，`2000`→`"two thousand"`，`2005`→`"two thousand and five"`（能覆盖1960-2035这个范围内所有年份即可，不用做成通用到任意年份的算法）。
   - `spokenText` 例：day=21,month=3(March),year=2019 → `"the twenty-first of March, twenty nineteen"`。
   - `promptText`（standalone模式）= null。
   - `correctAnswer` = `"2019-03-21"`（`YYYY-MM-DD`零填充）。
   - 判分：解析用户输入——如果第一段是4位数字，按`YYYY-MM-DD`（分隔符支持`/`、`-`、`.`、空格）解析；否则按`DD/MM/YYYY`解析（2位年份按`00-35→20xx，36-99→19xx`换算）。解析出的`(year,month,day)`跟correctAnswer比较。

3. **time（时间）**
   - 随机生成 hour(0-23)，minute从`[0,5,10,...,55]`（5的倍数）里随机选。
   - 拼读规则：minute=0→`"{h12} o'clock"`；minute=15→`"quarter past {h12}"`；minute=30→`"half past {h12}"`；minute=45→`"quarter to {h12+1的12小时制}"`；minute<30且非15→`"{minute的英文数词} past {h12}"`；minute>30且非45→`"{(60-minute)的英文数词} to {h12+1}"`。最后拼上时段短语：hour<12→`"in the morning"`，12≤hour<18→`"in the afternoon"`，否则→`"in the evening"`。
   - 例：hour=15,minute=45 → `"quarter to four in the afternoon"`。
   - `correctAnswer` = 24小时制`"HH:MM"`零填充，如`"15:45"`。
   - 判分：如果用户输入带am/pm（不区分大小写、允许有无空格），按12小时制转24小时制再比较；不带am/pm则按用户输入直接当24小时制比较（`"15:45"`）。

4. **money（金额）**
   - 随机选货币符号（`£`或`$`，写成一个可扩展的数组常量，方便以后加`€`）。
   - 随机生成整数部分(1-500)，小数部分50%概率是0（整数金额），50%概率是随机两位数。
   - 需要一个`numberToWords(n)`辅助函数（支持0-999即可，够用来拼整数部分和两位小数部分）。
   - `spokenText`：小数部分非0时 → `"{整数部分words} pounds and {小数部分words} pence"`（`$`对应`dollars`/`cents`）；小数部分为0时 → `"{整数部分words} pounds"`，不出现"and X pence"这半句。
   - 例：`£29.99` → `"twenty-nine pounds and ninety-nine pence"`。
   - `correctAnswer` = `"£29.99"`（符号+两位小数，整数金额也要补`.00`）。
   - 判分：只校验数值本身——从用户输入里剥离货币符号/单位词后转成数字，跟correctAnswer的数值部分比较（保留2位小数比较），**不强制要求用户输入货币符号**，这是刻意简化（后续如果用户觉得需要连符号一起判，再加严）。

5. **phone（手机号码）**
   - 生成英国手机号格式：`07` + 9位随机数字，共11位，如`"07911234567"`。
   - `spokenText`：把11位数字逐位转成英文数词并用`", "`连接，如`"zero, seven, nine, one, one, two, three, four, five, six, seven"`（保证每一位都被单独朗读，不要把整串数字交给TTS当一个大整数读）。
   - `correctAnswer` = 纯数字字符串`"07911234567"`。
   - 判分：用户输入去掉所有非数字字符（空格）后跟correctAnswer比较。
   - 注：真实电话号码朗读里常见"double five"这种重复数字简化表达，这版先不做（标记为后续可加的优化点，不在本轮验收范围内）。

### 对话测验：句子模板

新增 `DIALOGUE_TEMPLATES`（放在同一个`numberDrill.js`里），每个subtype配置对应category + 至少2条模板句子，`{V}`会被替换成该category生成的自然语言表达（即上面各category规则里的"spokenText"那段短语）：

| subtype | category | 模板句子示例（至少写2条，可以自己再加） |
|---|---|---|
| general | number | "There are {V} students enrolled in the course this year." / "The workshop had {V} participants in total." |
| money | money | "The total comes to {V}." / "She paid {V} for the repair." |
| phone | phone | "You can reach the office on {V}." / "My new number is {V}." |
| birthday | date | "Her birthday is on {V}." / "His birthday falls on {V}." |
| date_of_birth | date | "My date of birth is {V}." / "He was born on {V}." |
| deadline | date | "The application deadline is {V}." / "Please submit your form by {V}." |
| anniversary | date | "Their wedding anniversary is on {V}." / "They celebrate their anniversary on {V}." |
| movie_release | date | "The film will be released on {V}." / "The movie comes out on {V}." |
| date | date | "The meeting is scheduled for {V}." / "The event will take place on {V}." |
| mixed | 随机 | 每次生成题目时先从上面9个（不含mixed自己）随机选一个subtype，再走该subtype的逻辑 |

对话模式下，`spokenText` = 整句模板替换后的完整句子（TTS一次性朗读整句，这是对话测验比独立测验更难的地方——数字被淹没在语境里）；`promptText` = 同一句子，**只在用户提交答案之后才在界面上显示**（听的时候不能看到文字，纯听力），用于错题本回顾时展示"当时听的是哪句话"。`correctAnswer`/判分逻辑完全复用底层category的规则，不单独写一套。

### API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/numbers/question | body `{mode, category, subtype}` → 生成一题存入内存，返回 `{questionId, spokenText}`。**响应体禁止包含correctAnswer或promptText**（防止提前泄题，即使是自己用也要养成不泄题的习惯，方便以后如果给学生用） |
| POST | /api/numbers/answer | body `{questionId, userAnswer, examSessionId?}` → 判分、写入`number_drill_attempts`、内存里删掉该questionId（一次性作答，同一个questionId不能提交第二次，第二次返回400）。返回 `{isCorrect, correctAnswer, promptText}` |
| GET | /api/numbers/mistakes?limit=20 | 最近的错题列表，按attempted_at倒序 |
| GET | /api/numbers/stats | 总体正确率、各category正确率、总答题数 |
| POST | /api/numbers/exam/start | body `{questionCount, categories}` → 返回 `{examSessionId}`（`crypto.randomUUID()`，不用建表） |
| GET | /api/numbers/exam/:sessionId/summary | 汇总该session下的attempts：`{score, total, byCategory}` |

### 页面（复用现有SPA的配色/卡片风格，见前面"UI设计方向"章节，不要另起一套视觉语言）

1. **数字听力首页**：大按钮"开始数字测验"（点击直接进入number类别standalone练习）+ 两个小卡片"错题"/"统计" + "进阶测验"2x2网格（日期/时间/金额/手机号码，standalone模式）+ "考试模式"入口条 + "对话测验"网格（混合/一般数字/金额/手机号码/生日/出生日期/截止日期/纪念日/电影上映/日期，共10个，dialogue模式）。全部入口默认可点，没有PRO锁。
2. **单题练习界面**：喇叭按钮播放`spokenText`（可无限次重播，非考试模式下不限重播次数），文本输入框，提交后立刻显示对/错+正确答案，"下一题"按钮换同类别下一题。
3. **考试模式界面**：开始前选题量（默认10题，从5个category里混合随机出题），每题限时倒计时（默认20秒，超时按错误自动提交），重播按钮最多可点2次（第3次起置灰禁用），做完展示总分`X/N`+分类别正确率。
4. **错题页**：列出最近的错题（对话模式的题要显示`prompt_text`让用户看清当时听的是哪句话），每条一个"重新练习这一类"按钮，跳转到对应category/subtype的单题练习界面。
5. **统计页**：总正确率、各category正确率，样式仿照现有仪表盘的卡片（色块图标+大号数字）。

在首页底部导航或现有首页找一个入口位置加一个"数字听力"图标链接过去，具体放哪由你判断最不突兀的位置，不用纠结。

### 验收标准（必须给出可复现的证据，不接受"功能已实现/跑通不报错"）

写一个新的测试脚本 `test_number_drill.js`：

1. **date类别**：注入固定`now`使得可以稳定生成 day=21,month=3,year=2019 的题（测试时可以直接调用`generateQuestion`时传入一个mock随机数源，或者直接单测`ordinal`/`yearToWords`两个纯函数——`ordinal(21)`要等于`"twenty-first"`，`ordinal(11)`要等于`"eleventh"`，`ordinal(12)`要等于`"twelfth"`；`yearToWords(2019)`要等于`"twenty nineteen"`，`yearToWords(1990)`要等于`"nineteen ninety"`，`yearToWords(2000)`要等于`"two thousand"`）。断言用这三个值拼出的`spokenText`精确等于`"the twenty-first of March, twenty nineteen"`。
2. 对`correctAnswer="2019-03-21"`分别用`gradeAnswer`校验：提交`"21/03/2019"`、`"2019-03-21"`、`"21-03-2019"`都返回`true`；提交`"22/03/2019"`返回`false`。
3. **time类别**：断言 hour=15,minute=45 生成的`spokenText`等于`"quarter to four in the afternoon"`，`correctAnswer`等于`"15:45"`；`gradeAnswer`对`"3:45pm"`、`"15:45"`返回`true`，对`"3:45am"`返回`false`。
4. **money类别**：固定生成£29.99，断言`spokenText`等于`"twenty-nine pounds and ninety-nine pence"`；`gradeAnswer`对`"29.99"`、`"£29.99"`返回`true`，对`"29.90"`返回`false`。
5. **phone类别**：固定生成`"07911234567"`，断言`spokenText`按`", "`分割后有11段（逐位数字）；`gradeAnswer`对`"07911 234567"`、`"07911234567"`返回`true`，对少一位的`"0791123456"`返回`false`。
6. **number类别**：固定生成`"4829"`，`gradeAnswer`对`"4829"`、`"4,829"`返回`true`，对`"4830"`返回`false`。
7. 断言`DIALOGUE_TEMPLATES`里每个subtype至少有2条模板，且把`{V}`替换成任意字符串后，结果字符串里不再包含`"{V}"`这个占位符。
8. curl实测（贴出响应体作为证据）：
   - `POST /api/numbers/question` 响应JSON里**不含**`correctAnswer`或`promptText`字段。
   - 用拿到的`questionId`提交一个错误答案到`/api/numbers/answer`，断言响应`isCorrect:false`且带出`correctAnswer`；用**同一个**`questionId`再提交一次，断言返回400。
   - `GET /api/numbers/mistakes`能看到刚才那条错题。
   - 连续提交2题（1对1错），`GET /api/numbers/stats`里对应的总正确率数值等于50%。
   - `POST /api/numbers/exam/start`拿到`examSessionId`，用这个session提交3道题（2对1错），`GET /api/numbers/exam/:id/summary`断言`score=2, total=3`。
9. 回归测试：跑一遍现有的`test_srs.js`/`test_spelling.js`/`test_practice.js`/`test_speech.js`/`test_ui.js`/`test_listening.js`，确认全部依然通过，18+21张现有卡片数据不受影响。
10. 手机浏览器（或Chrome移动模式）打开新增的"数字听力"入口，截图确认无横向滚动、视觉风格跟首页一致（暖色卡片、圆角、配色沿用现有变量）。

本轮**只在本地** `D:\Codex工作区\ielts-review-tool` 开发和测试，不部署、不碰PM2、不推GitHub。改完在本地起一个开发服务器供用户在浏览器直接测试，等用户确认没问题再进入部署轮次。

## 数字听力模块：分类配色 + 喇叭点击打断输入（2026-09-09 追加，用户实测反馈）

用户用手机截图反馈两个问题：

### 问题1：所有类型的测验区块视觉上完全一样，分不清类型
现在"进阶测验"（日期/时间/金额/手机号码）和"对话测验"（10个subtype）的每个入口卡片都是纯白底+同一个粉色箭头，用户要求**用不同颜色区分不同类型**。参照用户给的Numblr原图配色逻辑（日期=红色日历图标、时间=蓝色时钟图标、金额=绿色$图标、手机号码=紫色电话图标），定下这套配色方案：

| category | 主题色 hex | 图标 |
|---|---|---|
| number（一般数字） | `#FF8A65`（复用现有听力橙） | `#` |
| date（日期） | `#FF6B6B`（珊瑚红） | 📅 |
| time（时间） | `#5B9DFF`（天蓝，新增） | 🕐 |
| money（金额） | `#3CBE8B`（薄荷绿，新增，区别于阅读色4ECDC4避免混淆） | 💰 |
| phone（手机号码） | `#9C8CFB`（复用现有写作紫） | 📞 |

在CSS里新增对应的自定义属性（放进现有变量体系旁边，比如 `--num-number` `--num-date` `--num-time` `--num-money` `--num-phone`），每个"进阶测验"入口卡片：左侧一个圆形/圆角色块图标（用上表的主题色做背景，图标用对应emoji或简单SVG），卡片本身可以保留白底，但加一条对应主题色的左边框或图标色块，明确用颜色区分开——不要做成花花绿绿的整块背景，延续现有"白卡片+色块图标点缀"的克制风格（同首页统计卡片的视觉语言，不要另起一套）。

"对话测验"的10个subtype入口，按其底层category上色（对照关系见前面《对话测验：句子模板》章节的subtype→category映射表）：
- `general` → number橙+`#`图标
- `money` → 金额绿+💰图标
- `phone` → 电话紫+📞图标
- `birthday`/`date_of_birth`/`deadline`/`anniversary`/`movie_release`/`date` 这6个都是日期语义的变体 → 统一用date红色块，但各自换一个贴合语义的emoji图标区分（🎂生日、👶出生日期、⏰截止日期、💍纪念日、🎬电影上映、📅日期），让用户一眼能看出"这6个都是日期类，只是场景不同"
- `mixed`（混合）→ 用一个明显区别于其他5种主题色的中性处理（比如灰紫色`#8E8A94`背景+🔀图标，暗示"随机混合"，不要用任何一个具体category的颜色，否则会让用户误以为mixed偏向某个类型）

"独立数字测验"入口（点击"开始数字测验"进入的那个number类别页面）、考试模式入口条，也按各自类别/性质配上对应颜色（考试模式本身不对应单一category，可以用现有首页头图的珊瑚橙→樱花粉渐变，呼应"进阶/正式"的分量感）。

### 问题2：点击喇叭播放按钮后，无法继续在答案输入框里打字，必须先点别处
用户原话："点击喇叭的时候，我仍然能够在输入框输入答案，而不是把鼠标点击到别的地方，就无法输入了，点击喇叭不影响输入"。

**这不是单纯的焦点丢失**（焦点丢失顶多是"再点一下输入框就好"，用户描述的是点完喇叭之后输入框整个进入某种不可用/失效状态，必须先点击页面别的地方才能恢复）。大概率原因：播放按钮的点击处理函数触发了整个练习面板的重新渲染（比如调用了跟"下一题"共用的同一个 `renderQuestion()` / 整段 `innerHTML` 重建逻辑），导致输入框DOM节点被销毁重建，用户当前的焦点/输入状态跟着失效。

**修复要求**：
1. 自己去看 `public/numbers.js` 里播放按钮（`#number-play`）的点击处理函数在做什么，确认是否触发了不必要的DOM重建。播放喇叭**只应该**做一件事——调用 `speechSynthesis` 朗读，不应该触发练习面板任何部分的重新渲染，不应该改变/替换 `#number-answer` 输入框（或考试模式下对应的输入框）的DOM节点，不应该把输入框设为disabled/readonly。
2. 播放按钮本身如果是 `<button>` 且处于 `<form>` 内，要显式加 `type="button"`，避免触发表单提交/隐式reset。
3. 修完后，用户应该能做到：点击喇叭听题 → 不点别处、直接在输入框里连续打字 → 正常输入 → 提交。反复点喇叭重播多次，中途穿插打字，输入不能被打断或清空。

**验收标准（追加到 `scripts/test_number_browser.js` 现有的CDP浏览器测试里，不要另起一套测试机制）：**
1. 颜色断言：用CDP的 `getComputedStyle` 断言 `--num-date`/`--num-time`/`--num-money`/`--num-phone`/`--num-number` 这几个CSS自定义属性的值分别等于上表定的hex；再断言"进阶测验"里"日期"入口卡片实际渲染出来的图标色块背景色跟 `--num-date` 一致（不能只是定义了变量没真的用上）。
2. 喇叭不打断输入：**必须用真实的 `Input.dispatchMouseEvent`（获取按钮实际 `getBoundingClientRect()` 坐标后模拟真实鼠标按下+抬起）点击喇叭按钮，不能用 `element.click()` 这种JS合成点击**——因为合成点击不会触发浏览器原生的焦点转移语义，测不出真实的这个bug。点击喇叭后，紧接着用 `Input.dispatchKeyEvent` 模拟真实键盘按键，逐字符输入一个测试答案到 `#number-answer`，断言输入框最终的 `.value` 等于输入的内容（证明确实能直接打字，不需要先点别处）。这一步在进入练习页面首次播放时测一次，再点一次喇叭重播后紧接着追加打字测一次，两种情况都要覆盖。
3. 回归：现有 `test_number_drill.js` 全部10项 + 之前 `scripts/test_number_browser.js` 里的既有断言不能因为这次UI改动而挂掉，跑一遍确认。
4. 移动端390×844截图（复用现有screenshot辅助函数）：新增一张"进阶测验+对话测验"页面截图，人工看一眼确认5种颜色能明显区分开（不需要写自动化断言，截图留证据即可，跟前面章节"手机浏览器截图无横向滚动"的验收方式一致）。

不部署、不碰PM2、不推GitHub，继续用本地开发服务器验证。

## 开源前置：可选访问口令（2026-09-10，为公开GitHub仓库做准备）

用户决定把这个仓库从private切成public开源。原设计"不做登录鉴权"（见前面"技术方案"章节）是基于"仓库私有、没人知道地址"的前提；仓库一旦公开，SPEC.md里写明的部署地址+"无鉴权"这个事实会被所有人看到，等于公开一个可以任意读写用户真实数据的接口。**必须在公开仓库之前，给线上实例补一层轻量访问口令**，同时不能破坏"开源出去给别人自己部署"时的开箱体验（别人自己部署、没配置口令的话，应该继续保持现在这种无鉴权直接用的状态，不能强制要求所有自部署者都必须配置口令）。

### 方案：环境变量控制的可选共享口令
- 服务端读取环境变量 `IELTS_ACCESS_TOKEN`。**未设置该环境变量时，行为跟现在完全一样，不做任何鉴权校验**（保证本地开发、现有测试套件、以及别人开源自部署且不关心鉴权的场景，都不用改一行代码或配置）。
- **设置了该环境变量时**，所有 `/api/` 开头的请求（读和写都算，不区分GET/POST）必须在请求头带 `X-Access-Token`，值跟环境变量完全一致，否则返回 `401 {"error":"未授权"}`。静态资源（index.html/css/js/manifest等）不做拦截，始终可以直接访问（前端壳子要能加载出来才能弹口令输入框）。
- 这层校验放在Express中间件层面，在所有 `/api/` 路由**之前**统一拦截，不要在每个路由handler里分别判断。

### 前端配合
- 前端所有调用 `/api/` 的fetch封装成统一的一个函数（如果现在还没有统一封装，这次顺手抽出来），自动从 `localStorage` 读取一个约定好的key（比如 `ielts_access_token`）并作为 `X-Access-Token` 请求头带上（不管有没有值都带，没配置口令的部署场景下，服务端本来就不校验这个头，带了也无所谓）。
- 任意一次API请求收到 `401` 响应时：清空 `localStorage` 里存的旧口令，展示一个简单的全屏输入框覆盖层（标题类似"请输入访问口令"，一个input+一个确认按钮），用户输入后存入 `localStorage`，关闭覆盖层并重试刚才失败的那次请求（或者简单点，重试失败直接提示用户手动刷新页面，不强制做自动重试也可以，选更简单的实现）。
- 这个输入框样式简单复用现有配色（暖色卡片风格）即可，不用做得多精致，这是给用户自己用的一次性设置，配置一次后 `localStorage` 会记住，之后不用重复输入。

### 验收标准
1. 不设置 `IELTS_ACCESS_TOKEN` 环境变量时，`test_number_drill.js`、以及既有全部回归测试（`test_srs.js`等6个）原样跑通，一行代码都不用改——证明默认行为完全不变。
2. 写一段新的测试（可以加进 `test_number_drill.js` 或新起一个 `test_access_token.js`，自行判断更合适的位置）：设置 `process.env.IELTS_ACCESS_TOKEN='test-secret-123'` 后启动服务，用curl验证：不带 `X-Access-Token` 头请求 `GET /api/cards` 返回401；带错误值的头返回401；带正确值 `test-secret-123` 的头返回200且能正常拿到数据；POST等写接口同样规则适用。
3. 验证静态资源（比如 `GET /`、`GET /styles.css`）在设置了环境变量的情况下依然直接200，不需要带口令头。
4. 前端401自动弹出口令输入框、输入后存入localStorage、后续请求自动带上正确头——用现有 `scripts/test_number_browser.js` 那套CDP浏览器测试的方式验证一遍（可以在其中新增一段场景，或者新起一个类似的浏览器测试脚本，自行判断），用真实浏览器环境跑通这个交互流程。

不碰PM2、不推GitHub、不改动线上服务器——这轮先在本地把代码和测试做完，验证过了再由用户/后续流程决定何时部署上线并在服务器配置真正的 `IELTS_ACCESS_TOKEN` 值。

## 三项用户实测反馈（2026-09-10）

### 反馈1：自由/单卡练习模式需要更醒目的"不计入进度"提示

排查过：线上真实数据库核实过SRS后端逻辑和数据完全正确（"记得"标记后箱位正确升级、`next_review_date`按箱位间隔正确计算，没有过早到期的卡片）。用户确认自己"不确定/两种都用过"来复习——今日复习入口（正式，会保存进度）、以及自由练习/卡片库单卡练习入口（故意设计成不保存进度，纯自测）。判断问题根源是**这两种模式视觉上区分度不够**，容易搞混进错入口导致以为复习了但进度没存。

现状：`state.reviewKind` 已经在区分 `'formal' / 'single' / 'free'`，但目前只体现在页面标题文字上（"今日复习"/"单卡练习"/"自由练习"），不够醒目。

**修复要求**：当 `state.reviewKind !== 'formal'` 时，在练习界面顶部加一条**贯穿整个练习过程、不会被忽略**的醒目提示条（比如警示色背景+图标，样式自定，但要跟正式复习的视觉区分足够大），文案类似"⚠️ 自由练习模式，本轮不影响复习进度"/"⚠️ 单卡练习模式，本轮不影响复习进度"，翻卡/提交拼写答案的整个过程中这条提示条都要保持可见，不能翻过一张卡就消失。

**验收标准**：
1. DOM/CDP断言：进入自由练习和单卡练习时，页面上存在这条提示条且文案正确；进入正式的"今日复习"时，这条提示条不存在。
2. 断言提示条在翻卡/提交答案后依然存在（不是只在进入时闪一下）。
3. 复用现有测试框架（`test_ui.js`或`scripts/test_number_browser.js`同类CDP方式，自行判断更合适的位置），现有测试回归通过。

### 反馈2：今日任务需要支持自由增删

现状：`daily_tasks` 只会自动生成写死的3项（听力/阅读/口语，来自 `DAILY_TASK_SKILLS` 常量），用户无法自定义。用户要求能自己加任务项、删任务项，不限定于这3个。

**数据模型**：新增 `task_templates` 表：
| 字段 | 说明 |
|---|---|
| id | PK |
| name | 任务名，不为空 |
| sort_order | 排序用 |
| active | 1=生效中，0=已删除（软删除，保留历史数据可追溯） |
| created_at | |

迁移：首次升级时，按当前约定的默认三项（听力/阅读/口语）写入 `task_templates`（sort_order 1/2/3, active=1），保证老用户升级后首页行为不变。`daily_tasks` 表加一列 `task_template_id`（FK），当天任务生成逻辑从"遍历写死的 `DAILY_TASK_SKILLS` 数组"改成"遍历当前 `active=1` 的 `task_templates`，按 `task_date + task_template_id` 唯一生成"。展示任务名时JOIN `task_templates` 拿当前名字（不做历史快照，改名即所有历史展示统一用新名字，简化设计）。

**API**：
- `GET /api/tasks/templates` — 列出所有 active 模板
- `POST /api/tasks/templates` `{name}` — 新增
- `PUT /api/tasks/templates/:id` `{name}` — 改名
- `DELETE /api/tasks/templates/:id` — 软删除（`active=0`），同时**直接删除"今天"这一条对应的 `daily_tasks` 行**（不论是否已完成），明天起也不再生成；已经过去的历史 `daily_tasks` 行不受影响，复盘统计里历史数据照常展示。

**页面**：首页"今日任务"卡片区域加一个"编辑"入口，点开是一个简单的模板管理界面：列表+每项删除按钮+底部"任务名输入框+添加"按钮。首页主视图打勾清单本身不用大改，只是数据源变成动态的。

**复盘统计**：现有"每日任务完成率"统计从写死遍历 `DAILY_TASK_SKILLS` 改成动态读取实际出现过的任务名（历史 `daily_tasks` 表里distinct的task_template_id对应的名字），保证新增自定义任务后统计不报错、不漏项。

**验收标准**：
1. 迁移后默认还是听力/阅读/口语三项，回归验证不变（curl `/api/tasks/today`）。
2. 新增自定义任务（比如"精读一篇文章"），curl验证当天`/api/tasks/today`返回里出现这一项且可勾选/取消勾选。
3. 删除一个任务模板，curl验证当天对应行从 `/api/tasks/today` 消失；构造一条"昨天"的历史daily_tasks数据，验证删除今天的模板不影响昨天历史记录在复盘统计里的展示。
4. 复盘统计接口在有自定义任务的情况下不报错，能正确统计到自定义任务的完成率。
5. 现有全部回归测试通过，18+张卡片、写作记录等既有数据不受影响。

### 反馈3：首页/导航隐藏"听力真题练习"入口

用户要求"删除真题练习"，确认方案是**只从导航/首页隐藏入口，不删除后端代码、数据库表、已导入的剑16 Test1音频/原文/答案数据**——以后想恢复随时能加回入口。

**修复要求**：
1. 首页去掉"听力真题练习"那张卡片（`public/app.js` 里 `data-go="listening"` 那个入口块）。
2. 检查其他地方（比如某个练习完成后的推荐跳转、底部导航等）有没有指向听力练习模块的入口，一并去掉，确保正常使用流程里找不到任何能点进听力真题练习模块的路径。
3. **后端路由（`/api/listening/*`）、`listening_sections`/`listening_tests`/`listening_attempts` 表、`data/audio/`下的音频文件，全部原样保留，不删除、不注释掉、不做任何破坏性改动**——只是前端不再展示入口。

**验收标准**：
1. 首页渲染结果里不再包含"听力真题练习"卡片/入口（DOM断言或截图）。
2. 确认没有任何其他可点击路径能到达 `data-go="listening"` 页面。
3. 后端 `/api/listening/overview` 等接口直接curl依然能正常返回数据（证明数据和接口都还在，只是前端不链接过去）。
4. 现有 `test_listening.js` 照常全绿（证明后端功能完好，回归不受影响）。

不部署、不碰PM2、不推GitHub、不改动线上服务器，本地验证完再由后续流程决定部署时机。
